{
    "type": "object",
    "additionalProperties": false,
    "required": ["telephone", "isPrimary", "legals", "confidence"],
    "properties": {
        "telephone": {
            "type": "string",
            "description": "telephone number"
        },
        "numberType": {
            "type": "string",
            "description": "the type of number (i.e. home / work / unknown)"
        },
        "isPrimary": {
            "type": "boolean",
            "description": "primary phone number for person"
        },
        "confidence": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "description": "the confidence in the phone number being related directly with profile"
        },
        "legals": {
           "type": "array",
           "minItems": 1,
           "description": "legal contracts governing the use of this address",
           "items": { "$ref": "legal.json#" }
        }
    }
}
